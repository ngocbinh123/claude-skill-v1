# pencil-toolkit (placeholder)

Reserved for Pencil-related skills. The exact scope is being defined and this
plugin is intentionally **not listed** in `.claude-plugin/marketplace.json` yet.

Once the scope is confirmed, add:

1. `.claude-plugin/plugin.json` (copy the shape from `../git-toolkit/`)
2. `skills/<skill-name>/SKILL.md` per skill (see `docs/authoring.md`)
3. An entry in `.claude-plugin/marketplace.json`, then run `node tools/sync-versions.js`
